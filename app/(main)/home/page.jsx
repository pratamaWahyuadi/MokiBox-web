"use client";

import Feed from "@/components/feed/Feed.jsx";

/**
 * Halaman FYP — fullscreen feed dengan scroll-snap.
 * Auth, layout, dan nav sudah ditangani oleh `app/(main)/layout.jsx`.
 */
export default function HomePage() {
  return <Feed />;
}